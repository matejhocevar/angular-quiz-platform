@import '../../../../variables.styl';

.questions-list {
  overflow-y: auto;

  .list-group {
    background: #fff;

    &.list {
      padding-top: 25px;
    }

    .list-group-item {
      border-radius: $border-radius;
      border: none;
      //border-top: 1px solid #dcdcdc;
      //border-bottom: 1px solid #dcdcdc;
      outline: none;
      cursor: pointer;

      * {
        display: inline-block;
      }

      &:hover {
        .drag-handle, .remove-handle {
          display: inline-block;
          opacity: 1;
          position: relative;
          z-index: 10;
        }
      }
    }
  }

  .drag-handle {
    //opacity: 0;
    cursor: grab;
  }

  .remove-handle {
    opacity: 0;
    cursor: pointer;
  }
}

